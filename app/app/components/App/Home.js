const React = require('react');
const { Link } = require('react-router');

const Home = () => {
    return (
        <div className="container text-center">
            <h1>Welcome!</h1>
        </div>
    );
};


export default Home;